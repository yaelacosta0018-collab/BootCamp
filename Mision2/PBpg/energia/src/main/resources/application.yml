spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/java_gip346c
    username: postgres
    password: XXXXX
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080 

